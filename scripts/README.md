# Scripts

Scripts must be deterministic, documented, and safe to run locally.

- `generate_reference_demo_ocl_report.py` compares OpenMRS reference demo concept bundles against Sihsalus content under `reference-sources/` and writes `reports/reference-demo-ocl-cleanup-report.csv`.
