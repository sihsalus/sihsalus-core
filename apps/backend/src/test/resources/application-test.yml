spring:
  datasource:
    url: jdbc:h2:mem:sihsalus;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH;NON_KEYWORDS=VALUE,USER
    username: sa
    password:
    driver-class-name: org.h2.Driver

sihsalus:
  admin:
    password: Admin123
