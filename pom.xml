<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>org.sihsalus</groupId>
    <artifactId>sihsalus-core-parent</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>Sihsalus Core Parent</name>
    <description>Modernized Sihsalus core foundation with OpenMRS compatibility boundaries.</description>
    <url>https://github.com/sihsalus/sihsalus-core</url>

    <modules>
        <module>core/openmrs-bom</module>
        <module>core/api</module>
        <module>core/liquibase</module>
        <module>modules/fhir2</module>
        <module>modules/webservices-rest</module>
        <module>modules/initializer</module>
        <module>modules/authentication</module>
        <module>modules/oauth2login</module>
        <module>modules/idgen</module>
        <module>modules/addresshierarchy</module>
        <module>modules/emrapi</module>
        <module>modules/o3forms</module>
        <module>modules/reporting</module>
        <module>modules/billing</module>
        <module>modules/stockmanagement</module>
        <module>modules/datafilter</module>
        <module>modules/fua</module>
        <module>modules/imaging</module>
        <module>modules/attachments</module>
        <module>modules/patientdocuments</module>
        <module>modules/cohort</module>
        <module>modules/queue</module>
        <module>modules/appointments</module>
        <module>modules/teleconsultation</module>
        <module>modules/bedmanagement</module>
        <module>modules/metadatamapping</module>
        <module>modules/openconceptlab</module>
        <module>modules/sihsalusinterop</module>
        <module>modules/event</module>
        <module>modules/calculation</module>
        <module>modules/htmlwidgets</module>
        <module>modules/reportingrest</module>
        <module>modules/serialization-xstream</module>
        <module>modules/ordertemplates</module>
        <module>modules/patientflags</module>
        <module>modules/legacyui</module>
        <module>apps/backend</module>
    </modules>

    <licenses>
        <license>
            <name>Mozilla Public License 2.0</name>
            <url>https://www.mozilla.org/en-US/MPL/2.0/</url>
        </license>
    </licenses>

    <scm>
        <connection>scm:git:https://github.com/sihsalus/sihsalus-core.git</connection>
        <developerConnection>scm:git:https://github.com/sihsalus/sihsalus-core.git</developerConnection>
        <url>https://github.com/sihsalus/sihsalus-core</url>
        <tag>HEAD</tag>
    </scm>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <maven.compiler.release>21</maven.compiler.release>

        <maven.version.minimum>3.9.6</maven.version.minimum>
        <maven.compiler.plugin.version>3.14.0</maven.compiler.plugin.version>
        <maven.enforcer.plugin.version>3.5.0</maven.enforcer.plugin.version>
        <maven.surefire.plugin.version>3.5.2</maven.surefire.plugin.version>
        <maven.failsafe.plugin.version>3.5.2</maven.failsafe.plugin.version>
        <jacoco.plugin.version>0.8.12</jacoco.plugin.version>
        <spotbugs.plugin.version>4.8.6.6</spotbugs.plugin.version>
        <spotless.plugin.version>2.43.0</spotless.plugin.version>
        <spring.boot.version>4.0.6</spring.boot.version>
        <openmrs.webservices.rest.version>3.4.1</openmrs.webservices.rest.version>
        <postgresql.version>42.7.11</postgresql.version>
        <h2.version>2.3.232</h2.version>
    </properties>

    <repositories>
        <repository>
            <id>openmrs-public</id>
            <name>OpenMRS Public Repository</name>
            <url>https://openmrs.jfrog.io/artifactory/public</url>
        </repository>
    </repositories>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.openmrs</groupId>
                <artifactId>openmrs-bom</artifactId>
                <version>3.0.0-SNAPSHOT</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring.boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.postgresql</groupId>
                <artifactId>postgresql</artifactId>
                <version>${postgresql.version}</version>
            </dependency>
            <dependency>
                <groupId>com.h2database</groupId>
                <artifactId>h2</artifactId>
                <version>${h2.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>${maven.compiler.plugin.version}</version>
                    <configuration>
                        <release>${maven.compiler.release}</release>
                        <encoding>${project.build.sourceEncoding}</encoding>
                        <proc>full</proc>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>${maven.surefire.plugin.version}</version>
                    <configuration>
                        <systemPropertyVariables>
                            <OPENMRS_APPLICATION_DATA_DIRECTORY>${project.build.directory}/openmrs-application-data</OPENMRS_APPLICATION_DATA_DIRECTORY>
                        </systemPropertyVariables>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-failsafe-plugin</artifactId>
                    <version>${maven.failsafe.plugin.version}</version>
                    <configuration>
                        <systemPropertyVariables>
                            <OPENMRS_APPLICATION_DATA_DIRECTORY>${project.build.directory}/openmrs-application-data</OPENMRS_APPLICATION_DATA_DIRECTORY>
                        </systemPropertyVariables>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.jacoco</groupId>
                    <artifactId>jacoco-maven-plugin</artifactId>
                    <version>${jacoco.plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>com.github.spotbugs</groupId>
                    <artifactId>spotbugs-maven-plugin</artifactId>
                    <version>${spotbugs.plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>com.diffplug.spotless</groupId>
                    <artifactId>spotless-maven-plugin</artifactId>
                    <version>${spotless.plugin.version}</version>
                    <configuration>
                        <java>
                            <googleJavaFormat/>
                            <removeUnusedImports/>
                            <trimTrailingWhitespace/>
                            <endWithNewline/>
                        </java>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>

        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
                <version>${maven.enforcer.plugin.version}</version>
                <executions>
                    <execution>
                        <id>enforce-toolchain</id>
                        <goals>
                            <goal>enforce</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <requireJavaVersion>
                                    <version>[21,22)</version>
                                </requireJavaVersion>
                                <requireMavenVersion>
                                    <version>[${maven.version.minimum},)</version>
                                </requireMavenVersion>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
